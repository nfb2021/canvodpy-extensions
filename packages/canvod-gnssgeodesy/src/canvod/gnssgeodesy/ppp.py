"""Precise Point Positioning (PPP) -- Phase 9, NOT SCOPED.

Tool choice pending: current candidates are the maintained
`rtklibexplorer/RTKLIB` fork (a pure-Python port, `rtklibexplorer/
rtklib-py`, may also be relevant if its core is cleanly callable) and
`PrideLab/PRIDE-PPPAR`. Neither license nor in-process-vs-CLI integration
shape has been verified against these specific repos yet. Do not add
scope here without a design discussion first.
"""

from __future__ import annotations
